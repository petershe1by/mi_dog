# 比赛赛段、暂停与断点恢复设计

## 目标

比赛程序按官方六个赛段运行。在赛段失败或出现危险时，允许经授权的语音命令或触摸区双击请求暂停；系统先停稳，在安全地面上回到趴下状态，并保存进度。随后可从当前赛段的安全检查点重试，完成后继续余下赛段。

正式比赛模式和家庭调试模式必须分开：

- `competition`：默认只允许“开始、暂停、继续当前赛段、重试当前赛段、停止”，不能语音跳到尚未完成的后续赛段。
- `practice`：允许按编号或名称选择任意赛段，便于在家单项测试。

> 规则补充允许语音或触控控制机器狗“继续比赛”，但未明确允许操作者用语音任意选段。正式启用“双击暂停”和“语音重试指定赛段”前，需要在技术会议或检录时得到裁判确认。

## 赛段划分

| 编号 | 官方赛段 | 建议恢复检查点 |
| --- | --- | --- |
| 1 | 石径探路 | 起点、石径入口、石径出口 |
| 2 | 荒野寻珠 | 搜索区入口；每次有效触球后记录目标集合 |
| 3 | 曲道冲锋 | 曲道入口、中段、出口 |
| 4 | 深隧寻珍 | 4A 限高杆/可乐；4B 障碍借道/悬挂橙；4C 第二限高杆/足球/桥入口 |
| 5 | 孤梁稳渡 | 桥前稳定区、桥后稳定区；桥面不设置“趴下”检查点 |
| 6 | 撷金建功 | 终局区入口、任务完成、冲线 |

保存的断点至少包含：当前赛段、子任务、已完成目标、局部定位置信度、最后安全检查点、暂停原因和比赛开始后的单调时钟时间。

## 状态机

```text
DOWN_WAITING --开始比赛--> STANDING_CHECK --通过--> RUNNING
     ^                                              |
     |                                              | 语音暂停/双击
     |                                              v
     +------ SAFE_LIE_DOWN <-- STOP_CONFIRMED <-- PAUSING
                |
                +--> DOWN_WAITING --继续/重试--> RECOVERING --> RUNNING

任何状态 --严重故障/停止比赛--> EMERGENCY_STOP
```

状态含义：

- `RUNNING`：赛段控制器拥有运动指令权。
- `PAUSING`：撤销赛段控制权，持续发送零速度，等待机身速度和足端状态确认。
- `SAFE_LIE_DOWN`：只有在姿态、落脚面和空间均安全时才执行趴下。
- `DOWN_WAITING`：所有运动命令锁定，只监听经过确认的恢复命令。
- `RECOVERING`：起立、自检、重新定位并恢复相应检查点。
- `EMERGENCY_STOP`：锁存停止，不能仅靠普通“继续”解除。

## 暂停动作顺序

1. 语音暂停口令（本机实测为“暂停”）或触摸区有效双击只产生一个 `pause_request`，不能直接调用趴下动作。
2. 立刻撤销导航/步态控制器的运动权限，发布零速度并等待停稳确认。
3. 保存断点；比赛总计时继续运行，软件暂停不能暂停官方 15 分钟计时。
4. 检查横滚/俯仰、足端接触、障碍距离和所处区域。
5. 在普通稳定地面执行趴下；在桥面、限高杆下、台阶边缘或定位失效时保持停稳姿态并报警，不能盲目趴下。
6. 语音播报当前赛段和可用恢复命令。

2026-08-08 真机验证表明，设备端已经识别手势：`protocol/msg/TouchStatus.touch_state=3` 对应官方头文件中的 `LPWG_DOUBLETAP_DETECTED`。一次双击可能重复发布数条状态 `3`，因此 supervisor 直接识别状态 `3`，并在首次接受后锁定 `1.5 s` 去重；不能按收到的消息条数自行拼接双击。实测单击没有产生 `touch_status` 消息。

2026-08-09 真人复测表明，头部双击会同时触发原厂电量播报和自定义 `PAUSE_TOUCH`；后者已关闭比赛门控并完成离线提示音。语音必须先唤醒，节点在 `dog_wakeup=true` 后主动打开 `continue_dialog`。这台狗把“启动程序”实际识别为“启动”，故真机配置采用精确短口令 `启动/恢复/暂停/终止`，不使用模糊匹配。

## 语音命令

先使用机器狗唤醒词，再说控制命令。当前官方手册的默认唤醒词是“铁蛋铁蛋”；最终仍应以本机实际语音节点为准。

| 命令 | competition | practice | 动作 |
| --- | --- | --- | --- |
| 开始比赛 | 是 | 是 | 从第一赛段启动并开始总计时 |
| 暂停比赛 | 待裁判确认 | 是 | 请求安全暂停 |
| 继续当前赛段 | 是 | 是 | 从最近安全检查点继续 |
| 重试当前赛段 | 待裁判确认 | 是 | 重置当前子任务后重试，不清除合法的既得进度 |
| 重新开始第 N 赛段 | 否，除非裁判明确允许 | 是 | 家庭单项调试 |
| 停止比赛 | 是 | 是 | 进入锁存急停状态 |

语音识别应采用命令白名单、置信度阈值和短时二次确认。机器狗自己的播报期间应暂时屏蔽识别，防止回声触发。

2026-08-09 根据小米官方 `interaction/cyberdog_audio` 源码及真机服务验证，
`asr_text` 只负责发布识别文本，并不会自动执行比赛命令或回复。自定义在线 TTS 在
本机返回 `status=1`，而离线内置提示音 `play_id=9000` 返回 `status=0`。因此比赛监督器
使用离线提示音确认命令已进入程序，并通过 `/mi_dog_real/operator_event` 发布结构化事件；
不能把“唤醒后机器狗有原厂回应”当作比赛程序已收到命令的证据。

## 恢复约束

- “重试赛段”是软件状态恢复，不会把机器狗瞬移到该赛段入口。
- 如果实际位置不在允许的恢复点，必须依靠视觉/里程计重新定位，或按裁判要求由工作人员放到允许位置；人工搬动可能产生罚分。
- 第二赛段应保留已确认碰到的目标，恢复后只重新寻找剩余目标。
- 第五赛段桥面只允许停稳或受控撤离，确认四足位于稳定地面后才趴下。
- 恢复前必须通过姿态、足端、前方障碍和定位置信度检查；任一失败则保持等待，不自动运动。

## 软件接口建议

```text
/mi_dog_real/operator/voice_text       std_msgs/String
/mi_dog_real/operator/touch_event      vendor touch message
/mi_dog_real/supervisor/pause_request  std_msgs/Bool
/mi_dog_real/supervisor/state          std_msgs/String
/mi_dog_real/supervisor/checkpoint     custom message or durable YAML/JSON
/mi_dog_real/race_enabled              std_msgs/Bool (已有安全门)
```

所有赛段节点只向仲裁器提交速度/姿态意图；只有 supervisor 处于 `RUNNING` 时仲裁器才允许指令到达底层。这样暂停不会依赖每一个赛段控制器都及时响应。

## 上机顺序

1. 机器狗趴下且架空，确认真实语音识别结果话题和触摸事件话题。
2. 仅打印命令与双击事件，不下发运动。
3. 验证安全门、状态机和断点持久化。
4. 地面验证“起立—零速暂停—安全趴下—恢复”，旁边有人持急停。
5. 分别验证六个赛段；桥面暂停逻辑最后测试。
6. 裁判书面或现场确认允许的操作后，再切换为 `competition` 配置。

2026-08-09 已完成第 3 步的软件部分：合成测试验证 `START -> stage 1`、赛段1完成后
持久化到赛段2、`PAUSE_TOUCH` 后拒绝赛段2完成、`CONTINUE` 后允许推进至赛段3；
进程重启加载赛段3但强制回到 `DOWN_WAITING`。随后测试检查点已重置为赛段1，并安装
启用默认无运动的 `mi-dog-real-sensor.service`。第 4 步的真实停稳/趴下控制尚未实现。

同日真人端到端验收通过：`铁蛋铁蛋→启动` 使 supervisor 从 `DOWN_WAITING` 进入
赛段1 `RUNNING`；真实头部双击上报 `touch_state=3` 并进入 `PAUSED`；
`铁蛋铁蛋→恢复` 从同一赛段回到 `RUNNING`。验收后已用内部暂停事件将系统留在
赛段1 `PAUSED`，服务保持 active，运动配置保持关闭。

随后完成了“安全趴下许可”的只读实现并在 ARM64 真机编译运行：持续检查里程计姿态和
速度、运动控制器故障、BMS 故障及数据新鲜度，全部通过后还要稳定保持 1.5 秒。
接入有线电源时许可固定为假，现场验证原因为 `wired_charging_motion_inhibited`。
该许可不会执行趴下；足端接触字段语义、地面边缘和空间检查仍是下一道关卡。

## 赛前必须向裁判确认

1. 开始后双击机身触摸区是否属于允许的“触控继续比赛”，能否用于暂停。
2. 语音是否只能说“继续”，还是允许“重试当前赛段/选择赛段”。
3. 失败恢复时允许放回哪些位置，由谁搬动，如何计罚。
4. 操作者是否可以一直持有独立急停装置。
