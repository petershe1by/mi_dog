# 待完成真机测试清单

更新日期：2026-08-12（Asia/Shanghai）

本文只记录尚未完成的真机验证和实现，勾选前必须保留日志、版本、配置、视频或物理结果证据。
Gazebo 六赛段已经完成，但不能作为下面任何真机项目的替代证据。

## 当前禁止条件

- 当前最后实测电量 17%，低于 supervisor 的 30% 硬门；物理运动测试建议从至少 50% 开始。
- BMS 报告 `power_wired_charging=true`，但本轮电量从 20% 降至 17%，必须先检查充电器、线缆和
  `charge` 口，确认电量持续上升。
- 正式配置仍为 `enable_motion=false`。不得为了让 UI 移动键生效而直接修改 YAML 绕过安全门。
- 真实运控测试时必须拔除充电线，并使用四脚离地架、防滑空场或厂家认可的防护工装。

## A. 已实现，只缺物理验收

- [ ] 在防护工装上连接真实 `motion_servo_cmd`，只发送零速度
  `SERVO_START → SERVO_DATA → SERVO_END`，保存实际话题和运控状态记录。
- [ ] 零速度会话中验证 UI/电脑 PAUSE 立即撤销 `run_allowed` 并发送 END。
- [ ] 零速度会话中验证服务重启先 STOP，重启后保持 `DOWN_WAITING/run_allowed=false`。
- [ ] 零速度会话中验证 0.30 秒命令 watchdog 和 supervisor 0.50 秒许可超时。
- [ ] 断开电脑链路时验证狗内置主控仍独立运行，且停止行为不依赖电脑在线。
- [ ] 电量充足、拔除充电线后，逐个验证六向 0.25 秒低速调试脉冲和 STOP；保存里程计、视频、
  实际位移和停止距离，不一次连续点击。
- [ ] 电量至少 50%、拔除充电线并清空周边后，从 UI 分别验证原厂 `111` 起立和 `101` 安全趴下；
  保存服务返回、`motion_status` 进度和现场视频，并确认所有闭锁条件都会禁用或拒绝按钮。
- [ ] 通过 UI 开启头部 RGB SSH-MJPEG，连续运行至少 2 分钟；记录实际 fps、延迟、流量、断线后
  进程回收和重连结果。源话题既有测量约 8.37–9.46 fps，不把该数值当作 UI 链路实测。
- [ ] 运行中模拟低于 30% 的真实 BMS 条件不可人为制造；在自然放电接近阈值时验证锁存 PAUSED，
  条件恢复不自动继续。

## B. 感知测试尚未完成

- [ ] 超声动态数据：不同材质、宽度、偏置和接近速度，记录原始距离、漏检率和停止阈值。
- [ ] 头部 ToF 真落差：在防坠工装上采集平地、台阶边缘、悬空和不同地面材质，只触发停止。
- [ ] 相机目标识别：石板、橙球、道路边界、隧道、桥、足球、球门和终点圈数据集。
- [ ] 真机定位：用 odom/视觉/雷达替换 Gazebo `/model_states` 和世界坐标，测量漂移和重定位。
- [ ] 传感器缺失、过期、遮挡和异常值时，验证 fail-closed、日志原因和恢复不自启。

## C. 功能尚未实现，之后才能测试

- [ ] 真机赛段 1：四块石板顺序、合法出口和边界控制。
- [ ] 真机赛段 2：橙球识别、去重触碰和路径规划。
- [ ] 真机赛段 3：曲道跟踪与实线边界保护。
- [ ] 真机赛段 4：隧道低姿态、碰撞保护和足球入门。
- [ ] 真机赛段 5：桥面对准、全足登桥、越线和跌落恢复。
- [ ] 真机赛段 6：足球越界、四足入圈、稳定停止和安全趴下。
- [ ] 自动停稳、地面/空间判断和安全趴下执行器闭环。
- [ ] 单次真机六赛段全程：不人工发送方向/速度，规定时间内完成并保存日志与视频。

## D. 赛事流程仍需确认或演练

- [ ] 记录暂停、重启是否继续计时及扣分规则。
- [ ] 记录跌落/出线后的放回位置与允许恢复的赛段。
- [ ] 记录重启后允许 CONTINUE 还是必须 START。
- [ ] 记录终点四脚入圈、趴下姿态和保持时间的裁判口径。
- [ ] 比赛场地完整彩排：离线启动、电脑暂停/重启、日志导出和备用方案。

## 每次测试必须记录

- Git commit、部署清单路径和 SHA256；
- 电量、温度、是否插充电线、场地和防护方式；
- 输入话题、参数、测试步骤、预期结果和实际结果；
- 是否连接真实运动话题、停止方式、最终姿态和异常；
- 原始日志、视频、数据文件路径和回滚方法。
