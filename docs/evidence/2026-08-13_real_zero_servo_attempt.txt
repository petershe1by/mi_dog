Test window: 2026-08-13 13:42-14:03 Asia/Shanghai
Starting local commit: efb419d
Robot target: mi@192.168.44.1
Field: user confirmed charge cable removed and field safe
Initial BMS: 95%, wired_charging=false, power_normal=true
Formal service: enable_motion=False; temporary separately named adapter only

Attempt 0 (no real motion output):
- Robot Python rejected subprocess.Popen(text=...).
- The acceptance script incorrectly printed PASS from its completed preflight subset.
- Fixed to universal_newlines=True and to require the complete result-key set with no exception.

Attempt 1 (real topic, original 0.05 m step height):
- Captured START then zero-velocity DATA; every captured velocity was zero.
- Initial mode transition raised the body from lying to standing; user observed no translation.
- The first collector serialized callbacks and misplaced END frames across phase boundaries.
- Final state PAUSED/run_allowed=false; temporary adapter exited.

Attempt 2 (collector fixed, original 0.05 m step height):
- Supervisor entered RUNNING and then locked PAUSED after a runtime input transition.
- Only END frames were observed in the measurement phases.
- XY odometry delta 0.000317 m; user observed no movement.

Attempt 3 (controller stabilization fixed, original 0.05 m step height):
- Real START -> zero-velocity DATA -> watchdog END succeeded.
- Explicit re-arm -> START -> zero-velocity DATA -> PAUSE END succeeded.
- All captured vel_des values were zero.
- Watchdog and PAUSE END were each observed within 0.50 seconds.
- XY odometry delta was 0.040369 m.
- User observed in-place stepping and some displacement, so acceptance is FAIL.
- Final state PAUSED/run_allowed=false; no temporary adapter remained.

Root cause:
- The adapter populated step_height=[0.05,0.05] on START, DATA and END regardless
  of velocity. The installed Xiaomi motion map labels motion_id 303 as slow walking,
  and its 303 preset uses step_height=[0.05,0.05]. The installed OFF preset uses
  step_height=[0.0,0.0]. Zero vel_des therefore did not mean no foot lift.

Fix prepared:
- START, END and a truly idle DATA frame now use step_height=[0.0,0.0].
- Configured nonzero step height is attached only while a translational or yaw
  command remains nonzero (including a bounded deceleration ramp).
- ARM64 colcon build passed in 1 min 59 sec.
- Isolated /mi_dog_test regression passed START/DATA/END ordering, watchdog,
  permission revoke, stale-command behavior, all-zero velocity, and new all-zero
  step-height assertions. It did not publish to the real motion topic.

Not yet accepted:
- The zero-step-height fix has not yet been retested on the real motion topic.
- No nonzero jog, autonomous course motion, or new posture command was attempted
  after the observed stepping.

Deployment after fix:
- Formal service restart used STOP first and returned to DOWN_WAITING/run_allowed=false.
- enable_motion remained False; no temporary adapter was running.
- Manifest: /home/mi/mi_dog_ws/state/deployment_manifest_20260813T140604+0800.txt
- Manifest SHA256: 1f9dab0bc7b30225df56e3ef299cfa41306f0fdd0961cc0a6fe8cc885b0c47da
- source_commit=4dc93f0
- Installed mi_dog_real_node SHA256:
  dba6509599d9690c25591ef52e4f644e8e40bd68ef945cdc487ae06ae19e19e0

Zero-step-height real-topic retest:
- Short canary: PASS in telemetry; START, four zero DATA frames and PAUSE END;
  all vel_des and step_height values zero; XY delta 0.000290 m. User observed
  no stepping, foot movement or translation.
- Full watchdog + PAUSE run: PASS in telemetry; all velocities and step heights
  zero; watchdog END and PAUSE END each within 0.50 sec; XY delta 0.012600 m.
- User observed body sway and was unsure whether a foot lifted slightly during
  the full run. Physical acceptance therefore remains NOT PASSED despite the
  telemetry result.
- Five-second post-test static baseline: 255 contact samples, all four channels
  constant at 0.5; 226 odom samples, max XY delta 0.000784 m; motion status
  remained switch_status=0,motion_id=112.
- The acceptance collector is being extended to record all four 50 Hz contact
  estimates during each future real gait session. Contact telemetry cannot
  override an observed physical lift; the next run needs close foot observation
  or a protective rig.

Foot-visible canary:
- Zero velocity and zero step height; 68 in-session contact samples.
- All four contact-estimate channels transiently reached 0.0 while motion status
  entered 303, despite XY delta only 0.006683 m.
- User closely observed the feet and explicitly confirmed that no foot left the
  ground. Therefore contactEstimate=0 during a gait transition is not sufficient
  evidence of physical lift on this firmware.
- Even without physical lift, entering gait 303 causes a mode/weight transition.
  The safer design is now: an idle Twist never emits START or DATA; it remains
  ended. An active session emits END as soon as its deceleration reaches zero.
- Updated isolated regression passed: idle request produced END only; isolated
  nonzero request still produced START/DATA with 0.05 m step height; timeout,
  permission revoke and stale-command behavior still produced the expected END.
- New real END-only acceptance is pending deployment and physical observation.
