# 2026 小米杯官方场地几何基线

来源：仓库上级目录中的官方《2026 年全国大学生计算机系统能力大赛—智能系统创新设计赛
小米杯赛题》PDF，正文“赛道布局”和第 3 页平面图。单位在配置中统一为米。

这份文件只固化官方固定结构，不代表真机场地已经验收。随机球、目标和障碍物位置必须由感知
确定；起点相对于 odom 的刚体变换必须在现场初始化。因此 `course_calibrated` 继续默认为 false。

## 坐标约定

- 图纸坐标系 `C`：原点位于起点矩形左下外角。`+x` 向图纸右侧，`+y` 向图纸上方，`z=0`
  为场地地面。官方起点箭头朝 `+x`：机器狗先从左向右穿过四块石板，再经右上角 0.60 m
  开口左转进入球区；初始朝向不是 `+y`。
- 里程计坐标系 `O`：启动后由真机 odom 给出，不假定与图纸重合。
- 现场初始化保存起点平移 `(o_x, o_y)` 和航向 `theta`；图纸点转换为 odom 点：
  `p_O = R(theta) * p_C + (o_x, o_y)`。
- 若起点姿态、尺度或航向置信度不足，控制器必须保持零输出；不得只用累计直线距离宣布赛段完成。

## 已固化的官方固定值

| 项目 | 参数 |
| --- | --- |
| 全场外轮廓 | `4.00 × 16.00 m` |
| 大部分净宽 | `1.00 m` |
| 起点区 | `4.00 × 1.00 m` |
| 石板 | 4 块；每块 `1.00 × 0.30 × 0.05 m`；间隔 `0.20 m` |
| 第一赛段出口 | 起点区右上角开口宽 `0.60 m`；中心约 `(3.70, 1.00)` |
| 第二赛段 | `4.00 × 4.00 m`；4×4 球阵；列距 `1.00 m`、行距 `0.64 m` |
| 第二赛段出口 | 球区左上角开口宽 `0.60 m`；中心约 `(0.30, 5.00)` |
| 球 | 直径 `0.20 m`；赛段二球底高 `0.20 m`；赛段四球底高 `0.60 m` |
| 曲道 | 净宽 `0.60 m`；图示纵向跨度 `2.00 m` |
| 第三赛段锚点 | 入口 `(0.30, 5.00)`，出口 `(3.70, 7.00)`；后腿清线参考 `(3.70, 7.30)` |
| 搜索区 | 外框 `4.00 × 4.00 m`、`y=7.00–11.00`；可搜索部分 `x=0–3.00`，3 条通道中心为 `x=0.50/1.50/2.50`，各宽 `1.00 m`、标注长度 `2.50 m` |
| 第四赛段固定分界 | 横向通道起于 `y=7.00`，竖道分隔端 `y=8.50`，竖道终点 `y=11.00`；物体实际坐标随机，不能写死 |
| 障碍随机范围 | `1.50 m` |
| 限高杆 | 长 `1.10 m`；截面 `0.10 × 0.10 m`；底部离地 `0.40 m` |
| 方块障碍 | 两个 `0.20 m` 方块；间隔 `0.20 m` |
| 球门 | `0.50 × 0.30 m`；深 `0.50 m` |
| 独木桥 | 宽 `0.50 m`，中心 `x=3.75`；`y=7.00–12.00`；高 `0.05 m`；跳下虚线 `y=11.50` |
| 第六赛段 | 区域 `y=12.00–16.00`，环道宽 `0.50 m`；中心线依次为底边 `y=12.25`、左边 `x=0.25`、顶边 `y=15.75`、右边 `x=3.75` |
| 第六赛段足球/终点 | 图示足球中心 `(1.00, 15.00)`；右侧出口约 `y=13.50`；终点圆中心约 `(3.75, 13.25)` |

对应 ROS 参数位于 `mi_dog_real/config/race_controller.yaml` 的 `geometry.*`；控制器代码中有同一
组保守默认值并执行键集合、正数、整数数量和结构关系校验。配置与代码不一致会被离线测试发现。

## 官方资料冲突

正文写黄色边沿宽 `0.15 m`、弯道部分 `0.10 m`；第 3 页图例写黄色边框宽 `0.10 m`。固定几何
仍分别记录 `0.15 m` 和 `0.10 m`，不据此改写工程图；按用户现场选择，运行时外边界保护使用
独立 `boundary_guard.keepout_m=0.08 m` 最终不可侵入余量。现场只需核对实物铺设方向和起点
刚体变换，不重新丈量工程图已有的整场尺寸。

## 尚不能从固定工程图替代的输入

- 每行每列一个橙球的当场排列；固定浅蓝球位置只用于排除，不用于猜橙球。
- 第四赛段三个目标、三个障碍物和球门的赛前随机位置。
- 起点放置误差、相机曝光/白平衡、odom 原点和航向漂移。
- 桥面、隧道和边界实物相对图纸的制造/铺设误差。

第三赛段的 S 弯没有标注各圆弧半径，因此只把入口和出口作为地图锚点，弯内必须用黄色双边界
闭环修正。第四赛段只能写死通道边界和中心线，不能把随机目标坐标固化为路线点。

上述输入没有置信度时必须停步。工程图参数完成不等于 `course_calibrated=true`，更不等于六赛段
物理验收完成。
