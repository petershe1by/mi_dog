# 2026 小米杯仿真验收矩阵

验收原则：日志状态与 Gazebo 物理现象必须同时成立；`smoke_test.sh` 只证明基础设施，不替代赛道冷回归。

## 最终冷启动回归

- 日期：2026-07-30/31（Asia/Shanghai）
- 官方基础镜像：`cyberdog_sim:v2026`
- 最终方案镜像：`sha256:3880dc86250ca0eb1c051c57b77161a691e0a37190c133278ca3f6170b02558d`
- 容器：`8d50cc1d76ba...`，无开发跳段参数，无人工运动控制
- 计时：`1785441207.058801304 - 1785440383.315309425 = 823.743492 s`
- 结果：正常 `stage 6 -> 7: football out and physical lie-down complete`；无 `deadline`

| 赛段 | 最终冷跑证据 | 状态 |
|---|---|---|
| 石径探路 | 四块实体门控 `(1.120,-.037)`、`(1.625,.169)`、`(2.123,.065)`、`(2.627,-.016)`；合法右侧开口 `x=2.921` | 通过 |
| 荒野寻珠 | 依次命中第 `2、3、4、1` 列，每列命中后永久排除；`stage 2 -> 3` | 通过 |
| 曲道冲锋 | 中心线路径完成；`stage 3 -> 4` | 通过 |
| 深隧寻珍 | 两根限高杆、可乐、借道障碍、悬挂球和语音动作齐全；足球2 `(2.122,11.464)` 入门 | 通过 |
| 孤梁稳渡 | 全足登桥、四足越线并执行官方跳下；桥后一次通用恢复物理站稳 | 通过 |
| 撷金建功 | 足球3在 `(2.200,12.877)` 持续越过南边界；四足入圈；pure-damper 后 `z<.10` | 通过 |
| 总体 | 单个冷容器，完整 `0→7`，823.743 秒，比内部保护线提前 71.257 秒 | 通过 |

## 鲁棒性回归

- 状态流中断：`simulator_state` 超过 0.75 秒未更新时立即零速；最终冷跑桥落地出现一次短暂陈旧，安全抑制生效。
- 跌倒恢复：低高度需持续 2 秒，姿态翻倒立即触发；恢复至少保持 7 秒，并用机身高度和朝上量确认物理站立。
- 故障注入：终点回程强制从 locomotion 切到 pure-damper 后，程序成功检测低高度，执行 `pure_damper -> recovery_stand -> locomotion`，随后入圈完成。
- 终点收尾：入圈后直接 pure-damper；定向测试约 8.14 秒、最终冷跑 11.099 秒达到 `z<.10`。DONE 后持续稳定 `(2.324,13.092,z=.054)`，不会被通用跌倒恢复重新站起。
- 足球边界：不使用固定初始位置；每次从 Gazebo 实体位置动态规划至安全南向通道，并要求越界连续保持 1.5 秒。
- 橙球阵：每次命中后记录列号，后续行不再扫描该列，避免重复碰撞得分无效。

## 第一关几何验收

官方 STL 世界坐标：四块石板分别为 `x=.592..892`、`1.092..1.392`、`1.592..1.892`、`2.092..2.392`，共同位于 `y=-.519...481`。黄实线横跨 `x=-.627..2.773、y=.482...592`；控制器在第四块后到 `x>2.88` 才允许向北转弯。

## 基础设施与复现

```bash
./scripts/build_image.sh
./scripts/run_race.sh
docker logs -f mi-dog-race
./scripts/smoke_test.sh
bash -n scripts/build_image.sh scripts/run_race.sh scripts/smoke_test.sh scripts/start_sim.sh
```

最终烟雾测试输出：`PASS: container, Gazebo, controller, autonomy, lidar, camera and audio assets`。正式验收必须删除并重建容器，不使用 `/reset_world`。
